# Dependencias
node_modules/

# Variables de entorno (SECRETOS)
.env

# Google Service Account (NUNCA subir a git)
service-account.json

# Google Apps Script (contiene credenciales hardcodeadas)
gas/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Sistema
.DS_Store
Thumbs.db


# Modelos entrenados: se regeneran en cada entrenamiento y se respaldan en
# Supabase (tabla ml_models). Llevan datos de rendimiento por persona, así que
# tampoco tienen por qué vivir en el repo.
pairing-model.json
vin-model.json
omisiones.json
